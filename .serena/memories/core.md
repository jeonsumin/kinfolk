# Kinfolk

- Monorepo work lives under workspace/frontend (Next.js) and workspace/backend-jwt (Spring Boot).
- Read `mem:frontend/core` for App Router UI and API-client conventions; read `mem:backend/core` for API, security, and persistence conventions.
- Shared task commands: `mem:suggested_commands`; coding completion gates: `mem:task_completion`; cross-module style: `mem:conventions`.