# Completion

- Run git diff --check and review only task-related changes.
- Frontend: npm run lint and npx tsc --noEmit; pre-existing lint failures must be identified separately.
- Backend: ./mvnw test (or compile when tests are not applicable). Java must be installed and available to the Maven wrapper.